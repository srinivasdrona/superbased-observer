# Observer Proxy Configuration — Arm OFF (compression disabled)
# Gate 2: SWE-bench Verified compression testing
# Port: 8831 (OFF arm, no compression)

[observer]
db_path = "compression-testing/gate2/observer-off.db"
log_level = "info"

[observer.watch]
enabled_adapters = []

[proxy]
enabled = true
bind_address = "0.0.0.0"
port = 8831
openai_upstream = "https://ai-shinof1261ai979822964896.cognitiveservices.azure.com"

[compression.shell]
enabled = false

[compression.indexing]
enabled = false

[compression.conversation]
enabled = false
