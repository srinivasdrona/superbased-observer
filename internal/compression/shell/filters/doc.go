// Package filters contains per-command shell-output filter implementations
// (git, go test, docker, kubectl, etc.) plus the TOML-based declarative
// filter engine. See spec §10 Layer 1. Implemented in Phase 5.
package filters
