// Package models contains the shared domain types used across the observer:
// Project, Session, Action, ToolEvent, TokenEvent, FileState, and related
// constants. Types here are deliberately minimal — storage-layer types that
// extend these live in internal/store.
package models
