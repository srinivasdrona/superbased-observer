apiVersion: v1
kind: Service
metadata:
  name: {{ include "observer-org.fullname" . }}
  labels:
    {{- include "observer-org.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: https
      protocol: TCP
      name: https
  selector:
    {{- include "observer-org.selectorLabels" . | nindent 4 }}
